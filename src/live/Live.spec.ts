
import { should } from "chai";
import { HLTV } from "hltv";


const MOCK_MATCHES = {

}

describe("A 'live' command", () => {

	it("should return a RichEmbed for each live match", async () => {
		// sinon.replace(HLTV, "getMatches", () => MOCK_MATCHES);
	});
})